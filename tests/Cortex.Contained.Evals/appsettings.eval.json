{
  "Eval": {
    "LlmProvider": {
      "Comment": "LLM credentials are auto-loaded from Bridge's cortex.yml + DPAPI secrets. Override via env vars (EVAL_LLM_API_KEY, EVAL_LLM_MODEL, EVAL_LLM_ENDPOINT) or appsettings.eval.local.json."
    },
    "Ollama": {
      "Endpoint": "http://localhost:11434",
      "EmbeddingModel": "qwen3-embedding:0.6b",
      "EmbeddingDimensions": 1024
    }
  }
}
